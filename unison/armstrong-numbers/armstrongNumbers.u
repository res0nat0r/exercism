--armstrongNumbers.isArmstrongNumber : Nat -> Boolean
--armstrongNumbers.isArmstrongNumber number 



tmp.l = Nat.toText 3345 |> toCharList |> indexed 
> l

> tmp.l |> List.head 
  |> Optional.map at1
  |> Optional.map Char.toText
  |> Nat.fromText




{-
(defn armstrong? [num] 
  (== num (reduce + (map #(Math/pow % (count (str num))) (split-number num))))
)
-}